# GitHub Account For This Repo

This repository is owned on GitHub by the `stoveruk-collab` account:

- Repo: `https://github.com/stoveruk-collab/personal-finance`
- Preferred git remote: `https://stoveruk-collab@github.com/stoveruk-collab/personal-finance.git`
- Commit email for this repo: `stoveruk@icloud.com`

Use `stoveruk-collab` when pushing, opening pull requests, or rotating repository secrets. Do not push this repo with the `kfinova` account.

## Quick Checks

Verify the current remote:

```bash
git remote -v
```

Verify the active GitHub CLI account:

```bash
gh auth status
```

If `gh` is active on the wrong account, switch it before GitHub operations:

```bash
gh auth switch -u stoveruk-collab
```
